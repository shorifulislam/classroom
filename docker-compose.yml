version: '2'
services:
  db:
    image: postgres
  web:
    build: ./Dockerfile
    command: bundle exec rails s -p 3000 -b '0.0.0.0'
    volumes:
      - .:/app
    ports:
      - "3000:3000"
    depends_on:
      - db
  ide:
    build: ./ide/.
    ports:
      - "80:80"